import React from 'react';

const NotFound = () => {
    return (
        <h1 style={{textAlign: 'center'}}>
            Ups... We haven`t page like this, try something else
        </h1>
    );
};

export default NotFound;